DEPLOY_HOST=10.8.45.10
DEPLOY_PORT=22
DEPLOY_USER=your-user
DEPLOY_PATH=/opt/lublin-assistant/brama
DEPLOY_SERVICE=lublin-assistant
HEALTH_URL=http://127.0.0.1:4000/health

# Runtime secret pushed into the server's brama-be/.env on deploy
# (rsync excludes .env, so it is provisioned here instead). Leave empty to skip.
NEO4J_PASSWORD=
